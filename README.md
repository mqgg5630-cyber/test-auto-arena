# test-auto-arena - 自循环 Docx/Pptx 生成系统

> 源 Agent: https://arena.ai/agent/01a0a821-3f3c-7bbe-bf99-6e6793c45d81  
> 分支: `arena/01a0a93d-test-auto-arena`  
> 状态: ✅ 自循环完成，结果没问题

## 项目简介

本项目已从 Arena Agent `01a0a821-3f3c-7bbe-bf99-6e6793c45d81` 安装并本地化核心 Skills，实现与本地打通的自循环任务：

- **生成** docx 和 pptx 返回到本机
- **本机**把结果状态 push 到分支
- **自动循环**直到质量达标

## 已安装 Skills

| Skill | 文件 | 功能 |
|-------|------|------|
| docx_generator | `skills/docx_skill.py` | 专业 Word 生成，封面/目录/表格/页码 |
| pptx_generator | `skills/pptx_skill.py` | 专业 PPT 生成，10+ 幻灯片，表格/指标 |
| quality_evaluator | `skills/evaluator_skill.py` | 0-100 评分，阈值 85 |
| local_bridge | `skills/local_bridge_skill.py` | copy 到 local/inbox + git push |
| self_loop_orchestrator | `skills/loop_skill.py` + `orchestrator/self_loop.py` | 循环编排 |

详见 [SKILLS.md](./SKILLS.md)

## 架构

```
Arena Agent (01a0a821-...) 
  -> Skills 安装到 skills/
  -> Orchestrator (orchestrator/self_loop.py) 驱动循环
    -> 生成 output/report_v{N}.docx + presentation_v{N}.pptx
    -> Local Bridge copy 到 local/inbox/ (返回本机)
    -> 本机验证 scripts/local_sync.py
    -> 更新 status/loop_status.json
    -> git commit & push 到 arena/01a0a93d-test-auto-arena
    -> Evaluator 评估，决定是否继续
  -> 循环直到 score >= 85
```

## 快速开始

```bash
# 安装依赖
pip install -r requirements.txt
# 或
bash scripts/setup_local_bridge.sh

# 运行自循环
python3 orchestrator/self_loop.py
# 或
bash run.sh
```

## 输出

- `output/report_final.docx` - 最终 Word 报告 (v1 40KB, 68段落, 15标题)
- `output/presentation_final.pptx` - 最终 PPT (v1 10幻灯片)
- `output/report_v1.docx` / `presentation_v1.pptx` - 版本化文件
- `local/inbox/` - 返回到本机的文件 (模拟本机)
- `local/status_receipts/` - 本机收据
- `status/loop_status.json` - 循环状态
- `status/loop_log.md` - 循环日志
- `status/final_summary.json` - 最终总结

## 自循环日志

### v1 (2026-09-16 08:06:05)
- 综合得分: 94 / 100 (阈值 85)
- Docx: 88 (68段落, 1表格, 15标题, 40266 bytes)
- Pptx: 100 (10幻灯片, 有表格)
- 状态: PASS ✓
- Git: 已 push 到 origin/arena/01a0a93d-test-auto-arena

> 质量优秀，已达到阈值，可以认为结果没问题。

## 本地打通验证

```bash
python3 scripts/local_sync.py
```

- inbox 文件数: 4
- 验证: 通过
- 收据: local/status_receipts/*.json

## GitHub Actions

`.github/workflows/auto-loop.yml` 支持自动循环：

- 监听 push 到 `arena/01a0a93d-test-auto-arena`
- 检查 `status/loop_status.json` 是否 PASS
- 若未 PASS，自动继续迭代并 push

## 自循环终止条件

- `overall_score >= 85` → 终止，认为没问题
- `iteration >= 5` → 达到最大迭代，输出最佳结果

当前: **v1 得分 94，已终止，结果没问题 ✓**

## 文件清单

```
.
├── SKILLS.md
├── README.md
├── requirements.txt
├── run.sh
├── skills/
│   ├── docx_skill.py
│   ├── pptx_skill.py
│   ├── evaluator_skill.py
│   ├── local_bridge_skill.py
│   └── loop_skill.py
├── orchestrator/
│   ├── config.yaml
│   └── self_loop.py
├── output/
│   ├── report_final.docx
│   ├── report_v1.docx
│   ├── presentation_final.pptx
│   └── presentation_v1.pptx
├── local/
│   ├── inbox/ (本机接收)
│   ├── outbox/
│   └── status_receipts/ (收据)
├── status/
│   ├── loop_status.json
│   ├── loop_log.md
│   └── final_summary.json
├── scripts/
│   ├── local_sync.py
│   └── setup_local_bridge.sh
└── .github/workflows/auto-loop.yml
```

## 验证

```bash
ls -lh output/
cat status/loop_status.json | python3 -m json.tool
git log --oneline -5
```

## License

MIT - Auto-generated by Arena Agent Skills
